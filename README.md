# Book Library Test Naming Kata

The production code is small on purpose. The tests pass, but many test names are vague, noisy, or hard to trust. Your job is to rename the tests so they explain the behavior clearly.

> [!TIP] Read the domain docs in the production classes, then improve the test names until each test tells a clear story.
